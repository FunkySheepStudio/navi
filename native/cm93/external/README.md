This directory is intended to hold the extracted OpenCPN CM93 sources needed
for porting into the `cm93_wrapper` library. Use the provided import script to
copy files from `native/external/OpenCPN` into this folder. The build system
will include `cm93.cpp` when present.
